---
interface Props {
  class?: string;
  as?: string;
}

const { class: className = '', as: Element = 'div' } = Astro.props;
---

<Element class:list={['max-w-container mx-auto px-4 sm:px-6 lg:px-8 w-full', className]}>
  <slot />
</Element>
